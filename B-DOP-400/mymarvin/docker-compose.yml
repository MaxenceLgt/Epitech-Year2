version: '3.8'

services:
  jenkins:
    container_name: mymarvin
    build:
      context: ./
    env_file:
      - path: ./.env
        required: true
    ports:
      - "8080:8080"
      - "50000:50000"
